import {Meta, Source} from '@storybook/addon-docs';

<Meta title="Getting Started/Installation" />

# Installation

With `yarn`:

<Source dark code={`yarn add react-weather-component`} />

With `npm`:

<Source dark code={`npm install react-weather-component`} />
