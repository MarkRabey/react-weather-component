import {
  ArgsTable,
  Meta,
  Canvas,
  Story,
  Description,
} from '@storybook/addon-docs';

import Weather from '..';

<Meta
  title="Components/Weather"
  component={Weather}
  argTypes={{
    apiKey: {table: {disable: true}},
    theme: {table: {disable: true}},
  }}
/>

export const Template = args => (
  <Weather apiKey={process.env.STORYBOOK_API_KEY || ''} {...args} />
);

<Canvas>
  <Story name="Light" args={{theme: 'light'}}>
    {Template.bind({})}
  </Story>
  <Story name="Dark" args={{theme: 'dark'}}>
    {Template.bind({})}
  </Story>
  <Story name="Monospace Dark" args={{theme: 'monospace-dark'}}>
    {Template.bind({})}
  </Story>
  <Story name="Monospace Light" args={{theme: 'monospace-light'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Weather} />
