import {
  ArgsTable,
  Canvas,
  Meta,
  IconGallery,
  IconItem,
  Story,
} from '@storybook/addon-docs';

import argTypes from './argTypes';
import icons from './icons';
import WeatherIcon, {WeatherIconProps} from '..';

<Meta
  title="Components/WeatherIcon"
  component={WeatherIcon}
  argTypes={argTypes}
/>

export const Template = args => (
  <WeatherIcon
    icon={`${args.icon}${args.variant}`}
    description={args.description}
  />
);

# WeatherIcon

Icons in categories. Each has day or night

## Day icons

<IconGallery>
  {icons.map(icon => (
    <IconItem name={`${icon.iconCode}d (${icon.name})`}>
      <WeatherIcon icon={`${icon.iconCode}d`} description={icon.description} />
    </IconItem>
  ))}
</IconGallery>

## Night icons

<IconGallery>
  {icons.map(icon => (
    <IconItem name={`${icon.iconCode}n (${icon.name})`}>
      <WeatherIcon icon={`${icon.iconCode}n`} description={icon.description} />
    </IconItem>
  ))}
</IconGallery>

<Canvas withSource="open">
  <Story name="WeatherIcon">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="WeatherIcon" />
