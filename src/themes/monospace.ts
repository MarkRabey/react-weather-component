import {Theme} from './types';
const monospaceTheme: Theme = {
  name: 'monospace',
  headingStyles: {
    fontFamily: 'monospace',
  },
  subheadingStyles: {fontFamily: 'monospace'},
  detailStyles: {fontFamily: 'monospace'},
};

export default monospaceTheme;
